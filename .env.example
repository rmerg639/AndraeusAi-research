# Andraeus AI - Environment Configuration
# =========================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control!
# The .gitignore already excludes .env files.

# =============================================================================
# GPU Server Configuration (for deploy_to_gpu.py)
# =============================================================================
# Set these to deploy experiments to a remote GPU server

ANDRAEUS_GPU_HOST=
ANDRAEUS_GPU_PORT=22
ANDRAEUS_GPU_USER=
ANDRAEUS_REMOTE_DIR=/root/andraeus-research

# =============================================================================
# Optional: HuggingFace Configuration
# =============================================================================
# Required for private/gated models (e.g., Llama)
# Get your token at: https://huggingface.co/settings/tokens

# HF_TOKEN=

# =============================================================================
# Optional: Weights & Biases (experiment tracking)
# =============================================================================
# WANDB_API_KEY=
# WANDB_PROJECT=andraeus-research
